import { createNanoEvents } from 'nanoevents';

export const SHOW_CAROUSEL = 'show-carousel';

export default createNanoEvents();

// this.unbind = emitter.on(SET_PRODUCTION_ACCOUNT_NOTE, this.updateNote);
// emitter.emit(COMPARES_UPDATED);
